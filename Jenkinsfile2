pipeline {
  agent any

  tools {
    nodejs "node"
  }

  stages {
    stage('Install packages') {
      steps {
        sh "npm install"
      }
    }
    stage('Running tests') {
      steps {
        sh "npm test"
      }
      steps{
        echo "Teste finalizado"
      }
    }
  }

}