pipeline {
  stages {
    stage('Install packages') {
      agent {
        docker {
          image 'node:18'
          reuseNode true
        }
      }
      steps {
        sh "npm install"
      }
    }
  }
}